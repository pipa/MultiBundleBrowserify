// Deps =========================================
import $ from 'jquery';
import React from 'react';
import ReactDOM from 'react-dom';

// Setting Global Vars ==========================
Object.assign(window, { React, ReactDOM, $, jQuery: $ });
